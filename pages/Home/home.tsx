import Header from "../../src/components/Header";

interface HomeProps {
  
};

export const Home = (props: HomeProps) => {  

  return (
    <article>
        <Header></Header>
    </article>
  );
};
